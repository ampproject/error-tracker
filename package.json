{
  "name": "error-tracker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "deploy": "gcloud app deploy",
    "lint": "eslint **/*.js --fix",
    "pretest": "npm run lint",
    "system-test": "samples test app",
    "test": "npm run system-test",
    "e2e-test": "samples test deploy"
  },
  "dependencies": {
    "@google-cloud/logging": "^1.0.2",
    "@google/cloud-debug-agent": "^0.9.3",
    "@google/cloud-trace-agent": "^0.6.3",
    "debug": "~2.6.3",
    "express": "~4.15.2",
    "http-status-codes": "^1.1.6",
    "winston": "^2.3.1",
    "chai": "latest",
    "chai-http": "latest",
    "mocha": "latest"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "^3.19.0",
    "eslint-config-google": "^0.8.0"
  }
}
